from django.urls import path
from . import views

urlpatterns = [
    # o nome servirá para fazer o 'reverse look-up' para as routes
    #path('', views.ingredients, name='ingredients-page'), 
    #path('', views.recipes, name='recipes-page'), 
]

